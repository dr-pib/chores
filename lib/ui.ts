// Shared base style for the supervisor action buttons on Today's Roster shift
// cards (Edit / Confirm / Delete). Keep these identical in format and vary only
// the color classes so the three buttons stay visually consistent.
export const ROSTER_ACTION_CLASS =
  'text-xs font-medium px-2.5 py-1 rounded-full transition-colors disabled:opacity-50'
